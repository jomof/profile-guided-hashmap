{
    "initializeCommand": "echo \"$DOCKER_TOKEN\" | docker login --username jomof --password-stdin",
    "image": "jomof/private:llvm-hash-dev-image",
    "postStartCommand": ".devcontainer/init.sh"
  }